{
  "name": "uk-memory-map",
  "version": "0.2.0",
  "private": true,
  "description": "A geographic UK memory atlas with a self-contained overview and optional online street map.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
