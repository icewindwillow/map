{
  "name": "uk-memory-map",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal, dependency-free foundation for a personal UK memory map.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
